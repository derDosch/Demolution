package com.demolution.game.entitis.interactions;

//ToDO only keep implementing if needet
//This is to let an NPC give an Item to the Player
/*
public class GiveItemInteraction implements NPCInteraction {
    private Item itemToGive;

    public GiveItemInteraction(Item itemToGive) {
        this.itemToGive = itemToGive;
    }

    @Override
    public void executeInteraction(NPC npc) {
        // Führe die Interaktion durch, z.B. gib das Item an den Spieler
        npc.getPlayer().addItem(itemToGive);
    }
}

 */
