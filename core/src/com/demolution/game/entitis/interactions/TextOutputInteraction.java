package com.demolution.game.entitis.interactions;
//ToDo Only keep Implementing if needed
//To let the NPC say something special one time
/*
public class TextOutputInteraction implements NPCInteraction {
    private String text;

    public TextOutputInteraction(String text) {
        this.text = text;
    }

    @Override
    public void executeInteraction(NPC npc) {
        // Führe die Interaktion durch, z.B. gib den Text aus
        System.out.println(text);
    }
}
 */